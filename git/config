[user]
    name = Alex Hirsch
    email = w4rh4wk@bluephoenix.at
[github]
    user = w4rh4wk
[alias]
    c = checkout
    co = commit
    d = diff
    ds = diff --staged
    l = log --all --graph --oneline --decorate
    s = status --short --branch
    g = grep --break --heading --line-number
    ff = merge --ff-only
    mnc = merge --no-ff --no-commit
    puff = pull --ff-only
    purr = pull --rebase
    ignore = update-index --assume-unchanged
    unignore = update-index --no-assume-unchanged
    ignored = !git ls-files -v | grep '^[[:lower:]]'
    gud = !cat $HOME/Pictures/praise_the_sun.txt
[color]
    ui = true
[core]
    editor = nvim
    autocrlf = false
[http]
    postBuffer = 524288000
[rerere]
    enabled = true
[push]
    default = simple
[grep]
    extendRegexp = true
    lineNumber = true
[diff]
    tool = vimdiff
[merge]
    tool = vimdiff
